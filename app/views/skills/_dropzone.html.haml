.col-md-5.large_height
  = form_tag '/dropzone_files', method: :post, class: 'dropzone drop_form', id: 'media-dropzone' do
    .fallback
      = file_field_tag 'media', multiple: true
.col-md-7
  #media-contents
    - dropzone_files.each do |media|
      - if media.file.file.exists?
        .thumbnail
          = image_tag media.file.url#, 'data-toggle' => "modal", 'data-target' => "#myModal"

-##myModal.modal.fade{"aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "5"}
-#  .modal-dialog{:role => "document"}
-#    .modal-content
-#      .modal-body